import { Text } from 'react-native';
import { Container } from './styles';

function Scan() {
    return (
        <Container>
            <Text>Scan</Text>
        </Container>
    );
};

export default Scan;